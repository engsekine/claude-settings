test@example.com
password123

対応中
--------------
  1. エア消費率（SAC / ガス消費量）の自動計算 — いちばん
  のおすすめです。開始残圧・終了残圧・タンク容量・平均深
  度・潜水時間はすべて既に入力項目にあるので、純粋関数 1
  つで「このダイブの消費率
  ○L/分」が出せます。ダイバーが手計算している定番値で、ウ
  ェイト調整やタンク選びの判断材料になります。DB
  変更ゼロで 007 とまったく同じ構成（shared/lib +
  詳細画面表示）で作れます
--------------

広告を見るとダイビングログを追加できる機能

デイリーボーナス

広告なし、ログ無制限のサブスク

管理画面を追加したい



  2. ブランク日数の表示 — 「最後に潜ってから ○日」を TOP
  に。ショップで毎回申告する値で、daysUntil
  が既存なのでほぼタダです
  3. 統計の拡充 — 年別・月別の本数推移、水温×季節、最大深
  度の推移グラフ。get_dive_stats RPC の拡張で対応できます

  中規模 — 新テーブル / Storage が必要

  4. ダイブサイト（ポイント）マスタ — 今は location
  が自由入力なので「大瀬崎」「伊豆 /
  大瀬崎」が別物になります。マスタ化すると「このポイント
  で何本・平均透明度・ベストシーズン」が出せて、ログの蓄
  積価値が大きく上がります。todo
  の管理画面のマスタ管理対象としても自然で、rules/sql.md
  の dive_sites 設計例とも一致します
  5. 写真添付 — Supabase Storage +
  RLS。ログアプリの体感価値を最も上げる定番機能で、既存の
  公開ページ（publicSlug）との相乗効果もあります
  6. 検索・フィルタ強化 —
  期間・深度範囲・ダイブタイプ。現状は番号 / 日付 /
  場所のみです

  大きめ — 差別化要素

  7. ログのエクスポート（PDF / CSV） —
  紙ログ提出・バックアップ用途。todo
  のサブスク特典との相性も良いです
  8. ダイブコンピュータ取り込み（UDDF / CSV インポート）
  — Garmin や Shearwater
  のエクスポートを読めると手入力がほぼ消えます。入力の手
  間がログアプリ最大の離脱要因なので、長期的には本命です
  9. PWA / オフライン入力 —
  ボート上は圏外が普通なので、オフライン記録 →
  同期は実用性が高いです（複雑度も高め）

  次の一手なら 1 の SAC
  計算が、実装コスト最小・ダイバー価値大・既存パターン流
  用でいちばんコスパが良いと思います。中期では 4
  のサイトマスタが管理画面（todo）の題材も兼ねられます。

  やるものが決まったら /speckit-specify <機能の説明>
  で仕様化から始められます。



    完了後の片付けフロー

  # 1. worktree
  側の作業をコミット済みにしておく
  git -C .claude/worktrees/007-blank-days add
  -A
  git -C .claude/worktrees/007-blank-days
  commit -m "feat: ブランク日数の表示"

  # 2. develop
  に取り込む（メインリポジトリ側で実行）
  git merge feature/008-blank-days
  #    ※ PR 運用なら merge の代わりに push
  して GitHub で PR を作成

  # 3. worktree を外す
  git worktree remove
  .claude/worktrees/007-blank-days

  # 4. 役目を終えたブランチを削除
  git branch -d feature/008-blank-days