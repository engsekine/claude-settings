/** ポイント名の最大文字数（dives.location と同じ上限。DB CHECK 制約と同期） */
export const PLAN_LOCATION_MAX_LENGTH = 120;

/** メモの最大文字数（DB CHECK 制約と同期） */
export const PLAN_NOTES_MAX_LENGTH = 2000;

/** 持ち物項目名の最大文字数（DB CHECK 制約と同期） */
export const PACKING_ITEM_NAME_MAX_LENGTH = 60;
